<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GymCalc.Pages.CalculatorPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Calculator">

    <ScrollView>
        <VerticalStackLayout
            x:Name="CalculatorLayout"
            Padding="10"
            Spacing="10">

            <!--  Item 0: Exercise type selection  -->
            <Grid
                Padding="0,0,0,10"
                ColumnDefinitions="*,*"
                ColumnSpacing="10"
                RowDefinitions="Auto,Auto"
                RowSpacing="10">
                <Label
                    Grid.Row="0"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Text="Select exercise type:" />
                <Button
                    x:Name="BarbellButton"
                    Grid.Row="1"
                    Grid.Column="0"
                    Clicked="OnBarbellButtonClicked"
                    Style="{StaticResource BarbellButtonStyle}" />
                <Button
                    x:Name="DumbbellButton"
                    Grid.Row="1"
                    Grid.Column="1"
                    Clicked="OnDumbbellButtonClicked"
                    Style="{StaticResource DumbbellButtonStyle}" />
            </Grid>

            <!--  Item 1: Form  -->
            <Grid
                x:Name="CalculatorFormGrid"
                Padding="0"
                ColumnDefinitions="2*,*"
                RowDefinitions="Auto,Auto"
                RowSpacing="10">

                <!--  Row 0: Max weight  -->
                <Label
                    x:Name="MaxWeightLabel"
                    Grid.Row="0"
                    Grid.Column="0"
                    FontSize="16"
                    Text="Maximum weight (kg)"
                    VerticalTextAlignment="Center" />
                <Frame
                    x:Name="MaxWeightEntryFrame"
                    Grid.Row="0"
                    Grid.Column="1"
                    Style="{StaticResource ControlFrameStyle}">
                    <Entry
                        x:Name="MaxWeightEntry"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Secondary},
                                                          Dark={StaticResource Tertiary}}"
                        FontSize="16"
                        Keyboard="Numeric"
                        VerticalTextAlignment="Center" />
                </Frame>

                <!--  Row 1: Bar weight  -->
                <Label
                    x:Name="BarWeightLabel"
                    Grid.Row="1"
                    Grid.Column="0"
                    FontSize="16"
                    Text="Bar weight (kg)"
                    VerticalTextAlignment="Center" />
                <Frame
                    x:Name="BarWeightPickerFrame"
                    Grid.Row="1"
                    Grid.Column="1"
                    Style="{StaticResource ControlFrameStyle}">
                    <Picker
                        x:Name="BarWeightPicker"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Secondary},
                                                          Dark={StaticResource Tertiary}}"
                        FontSize="16"
                        VerticalTextAlignment="Center" />
                </Frame>
            </Grid>

            <!--  Item 2: Calculate button  -->
            <Button
                Clicked="OnCalculateButtonClicked"
                Style="{StaticResource ButtonStyle}"
                Text="Calculate" />

            <!--  Item 3: Error or other message  -->
            <Label
                x:Name="ErrorMessage"
                Padding="0"
                FontAttributes="Bold"
                FontSize="16"
                Text=""
                TextColor="Red" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
