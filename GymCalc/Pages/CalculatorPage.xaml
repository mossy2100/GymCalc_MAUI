<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GymCalc.Pages.CalculatorPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:app="clr-namespace:GymCalc"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Calculator">

    <ScrollView>
        <StackLayout
            x:Name="CalculatorLayout"
            Padding="{x:Static app:App.Padding}"
            Spacing="{x:Static app:App.DoubleSpacing}">

            <VerticalStackLayout x:Name="CalculatorForm" Spacing="{x:Static app:App.Spacing}">

                <!--  Exercise type  -->
                <Label FontSize="16" Text="Select exercise type:" />
                <Grid
                    ColumnDefinitions="*,*"
                    ColumnSpacing="{x:Static app:App.Spacing}"
                    RowDefinitions="Auto,Auto"
                    RowSpacing="{x:Static app:App.Spacing}">
                    <Button
                        x:Name="BarbellButton"
                        Grid.Row="0"
                        Grid.Column="0"
                        Clicked="OnBarbellButtonClicked"
                        Style="{StaticResource BarbellButtonStyle}"
                        Text="Barbell" />
                    <Button
                        x:Name="DumbbellButton"
                        Grid.Row="0"
                        Grid.Column="1"
                        Clicked="OnDumbbellButtonClicked"
                        Style="{StaticResource DumbbellButtonStyle}"
                        Text="Dumbbell" />
                    <Button
                        x:Name="MachineButton"
                        Grid.Row="1"
                        Grid.Column="0"
                        Clicked="OnMachineButtonClicked"
                        Style="{StaticResource MachineButtonStyle}"
                        Text="Machine" />
                    <Button
                        x:Name="KettlebellButton"
                        Grid.Row="1"
                        Grid.Column="1"
                        Clicked="OnKettlebellButtonClicked"
                        Style="{StaticResource KettlebellButtonStyle}"
                        Text="Kettlebell" />
                </Grid>

                <!--  Max weight  -->
                <Grid
                    x:Name="MaxWeightGrid"
                    ColumnDefinitions="2*,*"
                    ColumnSpacing="{x:Static app:App.Spacing}"
                    RowDefinitions="Auto">
                    <Label
                        x:Name="MaxWeightLabel"
                        Grid.Row="0"
                        Grid.Column="0"
                        FontSize="16"
                        Text="Maximum weight (kg)"
                        VerticalTextAlignment="Center" />
                    <Frame
                        x:Name="MaxWeightFrame"
                        Grid.Row="0"
                        Grid.Column="1"
                        Style="{StaticResource ControlFrameStyle}">
                        <Entry
                            x:Name="MaxWeightEntry"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Secondary},
                                                              Dark={StaticResource Tertiary}}"
                            FontSize="16"
                            Keyboard="Numeric"
                            VerticalTextAlignment="Center" />
                    </Frame>
                </Grid>

                <!--  Bar weight  -->
                <Grid
                    x:Name="BarWeightGrid"
                    ColumnDefinitions="2*,*"
                    ColumnSpacing="{x:Static app:App.Spacing}"
                    RowDefinitions="Auto">
                    <Label
                        x:Name="BarWeightLabel"
                        Grid.Row="0"
                        Grid.Column="0"
                        FontSize="16"
                        Text="Bar weight (kg)"
                        VerticalTextAlignment="Center" />
                    <Frame
                        x:Name="BarWeightPickerFrame"
                        Grid.Row="0"
                        Grid.Column="1"
                        Style="{StaticResource ControlFrameStyle}">
                        <Picker
                            x:Name="BarWeightPicker"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Secondary},
                                                              Dark={StaticResource Tertiary}}"
                            FontSize="16"
                            VerticalTextAlignment="Center" />
                    </Frame>
                </Grid>

                <!--  Machine starting weight  -->
                <Grid
                    x:Name="StartingWeightGrid"
                    ColumnDefinitions="2*,*"
                    ColumnSpacing="{x:Static app:App.Spacing}"
                    RowDefinitions="Auto">
                    <Label
                        x:Name="StartingWeightLabel"
                        Grid.Row="0"
                        Grid.Column="0"
                        FontSize="16"
                        Text="Starting weight (kg)"
                        VerticalTextAlignment="Center" />
                    <Frame
                        x:Name="StartingWeightFrame"
                        Grid.Row="0"
                        Grid.Column="1"
                        Style="{StaticResource ControlFrameStyle}">
                        <Entry
                            x:Name="StartingWeightEntry"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Secondary},
                                                              Dark={StaticResource Tertiary}}"
                            FontSize="16"
                            Keyboard="Numeric"
                            VerticalTextAlignment="Center" />
                    </Frame>
                </Grid>

                <!--  Plates one side only  -->
                <Grid
                    x:Name="OneSideOnlyGrid"
                    ColumnDefinitions="2*,*"
                    ColumnSpacing="{x:Static app:App.Spacing}"
                    RowDefinitions="Auto">
                    <Label
                        x:Name="OneSideOnlyLabel"
                        Grid.Row="0"
                        Grid.Column="0"
                        FontSize="16"
                        Text="Get plates for one side only"
                        VerticalTextAlignment="Center" />
                    <Switch
                        x:Name="OneSideOnlySwitch"
                        Grid.Row="0"
                        Grid.Column="1"
                        HorizontalOptions="Start" />
                </Grid>

                <!--  Calculate button  -->
                <Button
                    Clicked="OnCalculateButtonClicked"
                    Style="{StaticResource ButtonStyle}"
                    Text="Calculate" />

                <!--  Error message  -->
                <Label
                    x:Name="ErrorMessage"
                    Padding="0"
                    FontAttributes="Bold"
                    FontSize="16"
                    Text=""
                    TextColor="Red" />
            </VerticalStackLayout>

            <VerticalStackLayout x:Name="CalculatorResults" Spacing="{x:Static app:App.DoubleSpacing}" />

        </StackLayout>
    </ScrollView>
</ContentPage>
