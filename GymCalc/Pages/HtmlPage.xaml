<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GymCalc.Pages.HtmlPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:GymCalc.Pages"
    xmlns:components="clr-namespace:GymCalc.Components"
    x:DataType="pages:HtmlPage"
    Title="{Binding Title}">

    <!-- <WebView x:Name="WebView" Source="{Binding FileName}" /> -->
    <BlazorWebView x:Name="BlazorWebView" HostPage="wwwroot/index.html"
        BackgroundColor="{AppThemeBinding Light='White', Dark='Black'}">
        <BlazorWebView.RootComponents>
            <RootComponent x:Name="RootComponent" Selector="#app"
                ComponentType="{x:Type components:Main}" />
        </BlazorWebView.RootComponents>
    </BlazorWebView>

</ContentPage>
