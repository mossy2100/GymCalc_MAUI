<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GymCalc.Pages.PlatesPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:app="clr-namespace:GymCalc"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Plates">

    <ScrollView Padding="{x:Static app:App.Padding}" VerticalOptions="Start">
        <VerticalStackLayout x:Name="PlatesStackLayout" Spacing="{x:Static app:App.Spacing}">
            <Label Text="Select which plate weights (kg) are available:" />
            <Grid
                x:Name="PlatesGrid"
                ColumnSpacing="{x:Static app:App.Spacing}"
                RowSpacing="{x:Static app:App.Spacing}" />
        </VerticalStackLayout>
    </ScrollView>

    <!--  <Button  -->
    <!--  Grid.Row="2"  -->
    <!--  Grid.Column="0"  -->
    <!--  Text="Add new"  -->
    <!--  Clicked="OnAddNewPlateWeightClicked"  -->
    <!--  Style="{StaticResource ButtonStyle}" />  -->
    <!--  <Button  -->
    <!--  Grid.Row="2"  -->
    <!--  Grid.Column="1"  -->
    <!--  Text="Save"  -->
    <!--  Clicked="OnSavePlateWeightsClicked"  -->
    <!--  Style="{StaticResource ButtonStyle}" />  -->

</ContentPage>
