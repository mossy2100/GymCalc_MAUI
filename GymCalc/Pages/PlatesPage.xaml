<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GymCalc.Pages.PlatesPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:pages="clr-namespace:GymCalc.Pages"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Plates">

    <ScrollView Padding="10" VerticalOptions="Start">
        <VerticalStackLayout x:Name="PlatesStackLayout" Spacing="10">
            <Label Text="Select which plate weights (kg) are available:" />
            <Grid
                x:Name="PlatesGrid"
                ColumnSpacing="10"
                RowSpacing="{x:Static pages:PlatesPage.PlatesGridRowSpacing}" />
        </VerticalStackLayout>
    </ScrollView>

    <!--  <Button  -->
    <!--  Grid.Row="2"  -->
    <!--  Grid.Column="0"  -->
    <!--  Text="Add new"  -->
    <!--  Clicked="OnAddNewPlateWeightClicked"  -->
    <!--  Style="{StaticResource ButtonStyle}" />  -->
    <!--  <Button  -->
    <!--  Grid.Row="2"  -->
    <!--  Grid.Column="1"  -->
    <!--  Text="Save"  -->
    <!--  Clicked="OnSavePlateWeightsClicked"  -->
    <!--  Style="{StaticResource ButtonStyle}" />  -->

</ContentPage>
