
                <FlexLayout JustifyContent="SpaceBetween">
                    <controls:VisualStateButton Text="50%"
                        Style="{StaticResource PercentButtonStyle}"
                        VisualState="{Binding SelectedPercent,
                            Converter={StaticResource DefaultPercentVisualStateConverter},
                            ConverterParameter=50}"
                        Command="{Binding SelectPercentCommand}"
                        CommandParameter="50" />
                    <controls:VisualStateButton Text="60%"
                        Style="{StaticResource PercentButtonStyle}"
                        VisualState="{Binding SelectedPercent,
                            Converter={StaticResource DefaultPercentVisualStateConverter},
                            ConverterParameter=60}"
                        Command="{Binding SelectPercentCommand}"
                        CommandParameter="60" />
                    <controls:VisualStateButton Text="70%"
                        Style="{StaticResource PercentButtonStyle}"
                        VisualState="{Binding SelectedPercent,
                            Converter={StaticResource DefaultPercentVisualStateConverter},
                            ConverterParameter=70}"
                        Command="{Binding SelectPercentCommand}"
                        CommandParameter="70" />
                    <controls:VisualStateButton Text="80%"
                        Style="{StaticResource PercentButtonStyle}"
                        VisualState="{Binding SelectedPercent,
                            Converter={StaticResource DefaultPercentVisualStateConverter},
                            ConverterParameter=80}"
                        Command="{Binding SelectPercentCommand}"
                        CommandParameter="80" />
                    <controls:VisualStateButton Text="90%"
                        Style="{StaticResource PercentButtonStyle}"
                        VisualState="{Binding SelectedPercent,
                            Converter={StaticResource DefaultPercentVisualStateConverter},
                            ConverterParameter=90}"
                        Command="{Binding SelectPercentCommand}"
                        CommandParameter="90" />
                    <controls:VisualStateButton Text="100%"
                        Style="{StaticResource PercentButtonStyle}"
                        VisualState="{Binding SelectedPercent,
                            Converter={StaticResource DefaultPercentVisualStateConverter},
                            ConverterParameter=100}"
                        Command="{Binding SelectPercentCommand}"
                        CommandParameter="100" />
                </FlexLayout>
